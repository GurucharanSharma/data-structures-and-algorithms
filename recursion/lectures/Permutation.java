package recursion.lectures;

public class Permutation {
    public static void main(String[] args) {
        permutation(args[0], "", args[0].length() - 1);
    }

    static void permutation(String str, int i) {
        
    }
}
